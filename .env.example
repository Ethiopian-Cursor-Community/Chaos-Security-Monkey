# Required for attacker + remediation agents (Cursor SDK)
CURSOR_API_KEY=

# Optional — passed through to target-app
JWT_SECRET=chaos-demo-secret

# Render sets PORT automatically; local default is 5000 in monkey.ts / 3000 in target-app config
# PORT=5000
